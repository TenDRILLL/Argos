{
  "name": "bungoapishits",
  "version": "1.0.0",
  "description": "Shits with bungo api regards venerity",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build && copyfiles -u 1 src/html/* build",
    "buildrun": "npm run build && npm run run",
    "run": "cd build && node index.js",
    "test": "npm run build && cd build && node test.js",
    "deploy": "rmdir /Q /S deploy && tsc --build tsconfig-deploy.json && copyfiles -u 1 src/html/* deploy && scp -i key.pub -r ./deploy venerity@venerity.xyz:~/argos"
  },
  "author": "Ten#0010",
  "license": "ISC",
  "dependencies": {
    "@discordjs/rest": "^1.3.0",
    "@types/node": "^18.11.9",
    "axios": "^0.27.2",
    "body-parser": "^1.20.1",
    "copyfiles": "^2.4.1",
    "discord-api-types": "^0.37.19",
    "discord-interactions": "^3.2.0",
    "dotenv": "^16.0.3",
    "enmap": "^5.9.2",
    "express": "^4.18.2",
    "typescript": "^4.9.3"
  }
}
